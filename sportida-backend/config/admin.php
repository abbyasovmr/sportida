<?php
// config/admin.php

return [
    /*
    |--------------------------------------------------------------------------
    | IP Whitelist для админки
    |--------------------------------------------------------------------------
    | Оставьте пустым чтобы разрешить все IP
    */
    'allowed_ips' => env('ADMIN_ALLOWED_IPS', '') ? 
        explode(',', env('ADMIN_ALLOWED_IPS')) : [],

    /*
    |--------------------------------------------------------------------------
    | Требовать 2FA для админов
    |--------------------------------------------------------------------------
    */
    'require_2fa' => env('ADMIN_REQUIRE_2FA', true),

    /*
    |--------------------------------------------------------------------------
    | Время жизни сессии в минутах
    |--------------------------------------------------------------------------
    */
    'session_lifetime' => env('ADMIN_SESSION_LIFETIME', 120),

    /*
    |--------------------------------------------------------------------------
    | Автоматический выход при бездействии (минуты)
    |--------------------------------------------------------------------------
    */
    'idle_timeout' => env('ADMIN_IDLE_TIMEOUT', 30),

    /*
    |--------------------------------------------------------------------------
    | Максимальное количество параллельных сессий
    |--------------------------------------------------------------------------
    */
    'max_sessions' => env('ADMIN_MAX_SESSIONS', 3),

    /*
    |--------------------------------------------------------------------------
    | Логирование всех действий админов
    |--------------------------------------------------------------------------
    */
    'audit_logging' => env('ADMIN_AUDIT_LOGGING', true),

    /*
    |--------------------------------------------------------------------------
    | Уведомления о подозрительной активности
    |--------------------------------------------------------------------------
    */
    'security_alerts' => [
        'enabled' => env('ADMIN_SECURITY_ALERTS', true),
        'email' => env('ADMIN_ALERT_EMAIL', 'security@sportida.ru'),
        'threshold' => 5, // Уведомление после 5 подозрительных действий
    ],

    /*
    |--------------------------------------------------------------------------
    | Проверка паролей в базе утечек (Have I Been Pwned)
    |--------------------------------------------------------------------------
    */
    'check_breached_passwords' => env('ADMIN_CHECK_BREACHED_PASSWORDS', true),

    /*
    |--------------------------------------------------------------------------
    | Роли с повышенными привилегиями
    |--------------------------------------------------------------------------
    */
    'privileged_roles' => ['super_admin', 'admin'],

    /*
    |--------------------------------------------------------------------------
    | Действия которые всегда логируются
    |--------------------------------------------------------------------------
    */
    'audit_actions' => [
        'login',
        'logout',
        'failed_login',
        'password_reset',
        'password_changed',
        'unauthorized_access',
        'model_created',
        'model_updated',
        'model_deleted',
        'bulk_delete',
        'export',
        'import',
    ],
];
